# Base URL of the backend API. Leave UNSET for local development — Vite proxies "/api"
# to http://localhost:5000 (see vite.config.ts).
#
# In production (Vercel), set this to the deployed server's URL + /api, e.g.:
# VITE_API_URL=https://your-server.vercel.app/api
VITE_API_URL=
